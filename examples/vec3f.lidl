namespace gfx::yolo;

enum some_enum {
    a,
    b,
    c
}

struct vec3f {
    x: f32;
    y: f32;
    z: f32;
}

struct none_t {}

union maybe_int {
    val: i32;
    none: none_t;
}

struct have_a_str {
    name: string;
}